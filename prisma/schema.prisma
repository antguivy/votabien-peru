
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model alliancecomposition {
  id                                                               String            @id(map: "alliance_composition_pkey") @db.VarChar
  created_at                                                       DateTime          @default(now()) @db.Timestamptz(6)
  parent_org_id                                                    String?           @db.VarChar
  child_org_id                                                     String?           @db.VarChar
  process_id                                                       String?           @db.VarChar
  politicalparty_alliancecomposition_child_org_idTopoliticalparty  politicalparty?   @relation("alliancecomposition_child_org_idTopoliticalparty", fields: [child_org_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "alliance_composition_child_org_id_fkey")
  politicalparty_alliancecomposition_parent_org_idTopoliticalparty politicalparty?   @relation("alliancecomposition_parent_org_idTopoliticalparty", fields: [parent_org_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "alliance_composition_parent_org_id_fkey")
  electoralprocess                                                 electoralprocess? @relation(fields: [process_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "alliance_composition_process_id_fkey")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model attendance {
  id                String           @id @db.VarChar
  legislator_id     String           @db.VarChar
  date              DateTime         @db.Timestamptz(6)
  session_type      sessiontype
  created_at        DateTime         @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at        DateTime         @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  attendance_status attendancestatus
  notes             String?          @db.VarChar(500)
  legislator        legislator       @relation(fields: [legislator_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([attendance_status], map: "ix_attendance_attendance_status")
  @@index([legislator_id], map: "ix_attendance_legislator_id")
  @@index([session_type], map: "ix_attendance_session_type")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model background {
  id               String           @id @db.VarChar
  person_id        String           @db.VarChar
  publication_date String?
  type             backgroundtype
  status           backgroundstatus
  summary          String?
  sanction         String?          @db.VarChar(500)
  source           String           @db.VarChar(300)
  source_url       String?          @db.VarChar
  created_at       DateTime         @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at       DateTime         @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  title            String
  person           person           @relation(fields: [person_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([person_id], map: "ix_background_person_id")
  @@index([status], map: "ix_background_status")
  @@index([type], map: "ix_background_type")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model bill {
  id                     String              @id @db.VarChar
  number                 String              @unique @db.VarChar(50)
  title                  String?
  summary                String?
  submission_date        DateTime            @db.Timestamptz(6)
  created_at             DateTime            @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at             DateTime            @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  parliamentary_group_id String?             @db.VarChar
  approval_date          DateTime?           @db.Timestamptz(6)
  approval_status        billapprovalstatus  @default(PRESENTADO)
  sponsor                String?             @db.VarChar(150)
  period                 String?             @db.VarChar(50)
  legislative_session    String?             @db.VarChar(100)
  committees             String?
  document_url           String?             @db.VarChar
  title_ai               String?
  legislator_id          String              @db.VarChar
  coauthors              String?
  cosponsors             String?
  legislator             legislator          @relation(fields: [legislator_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  parliamentarygroup     parliamentarygroup? @relation(fields: [parliamentary_group_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([approval_date], map: "ix_bill_approval_date")
  @@index([approval_status], map: "ix_bill_approval_status")
  @@index([legislator_id], map: "ix_bill_legislator_id")
  @@index([parliamentary_group_id], map: "ix_bill_parliamentary_group_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model candidate {
  id                    String            @id @db.VarChar
  person_id             String            @db.VarChar
  electoral_process_id  String            @db.VarChar
  type                  candidacytype
  political_party_id    String            @db.VarChar
  electoral_district_id String
  status                candidacystatus
  created_at            DateTime          @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  list_number           Int?
  updated_at            DateTime          @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  active                Boolean
  electoraldistrict     electoraldistrict @relation(fields: [electoral_district_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  electoralprocess      electoralprocess  @relation(fields: [electoral_process_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  person                person            @relation(fields: [person_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  politicalparty        politicalparty    @relation(fields: [political_party_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  candidatemetrics      candidatemetrics?

  @@index([electoral_district_id], map: "idx_candidate_district")
  @@index([political_party_id], map: "idx_candidate_party")
  @@index([electoral_process_id, type, active], map: "idx_candidate_process_type_active")
  @@index([electoral_process_id], map: "ix_candidate_electoral_process_id")
  @@index([person_id], map: "ix_candidate_person_id")
  @@index([political_party_id], map: "ix_candidate_political_party_id")
  @@index([type], map: "ix_candidate_type")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model candidatemetrics {
  candidate_id               String    @id @db.VarChar
  total_previous_candidacies Int
  times_elected              Int
  political_experience_years Int
  total_parties_belonged     Int
  years_in_current_party     Float
  max_academic_level_score   Int
  has_postgraduate           Boolean
  declared_income_annual     Float
  declared_assets_value      Float
  total_legal_records        Int
  has_penal_sentences        Boolean
  has_alimentary_debts       Boolean
  last_updated               DateTime  @db.Timestamp(6)
  candidate                  candidate @relation(fields: [candidate_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model electoraldistrict {
  id          String       @id @db.VarChar
  name        String       @unique(map: "ix_electoraldistrict_name") @db.VarChar(100)
  code        String       @unique @db.VarChar(10)
  is_national Boolean
  created_at  DateTime     @db.Timestamptz(6)
  active      Boolean
  updated_at  DateTime     @db.Timestamptz(6)
  ubigeo      String?
  candidate   candidate[]
  legislator  legislator[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model electoralprocess {
  id                  String                @id @db.VarChar
  name                String                @db.VarChar(200)
  year                Int
  election_date       DateTime              @db.Timestamptz(6)
  active              Boolean
  created_at          DateTime              @db.Timestamptz(6)
  updated_at          DateTime              @db.Timestamptz(6)
  alliancecomposition alliancecomposition[]
  candidate           candidate[]

  @@index([year], map: "ix_electoralprocess_year")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model executive {
  id         String           @id @db.VarChar
  person_id  String           @db.VarChar
  role       executiverole
  start_date DateTime         @db.Timestamptz(6)
  end_date   DateTime?        @db.Timestamptz(6)
  end_reason endoftermreason?
  created_at DateTime         @db.Timestamptz(6)
  updated_at DateTime         @db.Timestamptz(6)
  ministry   String?          @db.VarChar
  person     person           @relation(fields: [person_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([person_id], map: "ix_executive_person_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model financingreports {
  id             String           @id @db.VarChar
  party_id       String           @db.VarChar
  report_name    String           @db.VarChar
  filing_status  financingstatus
  source_name    String           @db.VarChar
  source_url     String?          @db.VarChar
  report_date    DateTime         @db.Date
  period_start   DateTime         @db.Date
  period_end     DateTime         @db.Date
  created_at     DateTime         @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at     DateTime         @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  politicalparty politicalparty   @relation(fields: [party_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  partyfinancing partyfinancing[]

  @@unique([party_id, report_name])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model hito {
  id                BigInt   @id(map: "teamphoto_pkey") @default(autoincrement())
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  date              String?
  location          String?
  photo_url         String?
  photo_description String?
  index             BigInt?
  quote             String?
  label             String?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model legislator {
  id                      String                    @id @db.VarChar
  person_id               String                    @db.VarChar
  electoral_district_id   String                    @db.VarChar
  chamber                 chambertype
  start_date              DateTime                  @db.Timestamptz(6)
  end_date                DateTime?                 @db.Timestamptz(6)
  active                  Boolean
  institutional_email     String?                   @db.VarChar(255)
  created_at              DateTime                  @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  condition               legislatorcondition       @default(EN_EJERCICIO)
  updated_at              DateTime                  @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  elected_by_party_id     String                    @db.VarChar
  attendance              attendance[]
  bill                    bill[]
  politicalparty          politicalparty            @relation(fields: [elected_by_party_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  electoraldistrict       electoraldistrict         @relation(fields: [electoral_district_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  person                  person                    @relation(fields: [person_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  legislatormetrics       legislatormetrics?
  parliamentarymembership parliamentarymembership[]
  seatparliamentary       seatparliamentary[]

  @@index([electoral_district_id], map: "ix_legislator_electoral_district_id")
  @@index([person_id], map: "ix_legislator_person_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model legislatormetrics {
  legislator_id            String     @id @db.VarChar
  total_bills              Int
  bills_presentado         Int
  bills_en_comision        Int
  bills_aprobado           Int
  bills_rechazado          Int
  bills_retirado_por_autor Int
  approval_rate            Float?
  total_sessions           Int
  sessions_present         Int
  sessions_absent          Int
  sessions_justified       Int
  sessions_license         Int
  attendance_rate          Float?
  total_party_changes      Int
  days_in_current_group    Int?
  is_defector              Boolean
  total_legal_records      Int
  penal_records            Int
  ethical_records          Int
  civil_records            Int
  administrative_records   Int
  last_updated             DateTime   @db.Timestamptz(6)
  bills_en_proceso         Int
  legislator               legislator @relation(fields: [legislator_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model parliamentarygroup {
  id                      String                    @id @db.VarChar
  name                    String                    @unique(map: "ix_parliamentarygroup_name") @db.VarChar(200)
  acronym                 String?                   @db.VarChar(20)
  color_hex               String?                   @db.VarChar(7)
  logo_url                String?                   @db.VarChar
  description             String?                   @db.VarChar(1000)
  formation_date          DateTime?                 @db.Timestamptz(6)
  dissolution_date        DateTime?                 @db.Timestamptz(6)
  active                  Boolean
  created_at              DateTime                  @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at              DateTime                  @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  rules_url               String?                   @db.VarChar
  bill                    bill[]
  parliamentarymembership parliamentarymembership[]

  @@index([acronym], map: "ix_parliamentarygroup_acronym")
  @@index([active], map: "ix_parliamentarygroup_active")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model parliamentarymembership {
  id                     String             @id @db.VarChar
  legislator_id          String             @db.VarChar
  parliamentary_group_id String             @db.VarChar
  start_date             DateTime           @db.Timestamptz(6)
  end_date               DateTime?          @db.Timestamptz(6)
  change_reason          groupchangereason
  notes                  String?            @db.VarChar(1000)
  created_at             DateTime           @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  source_url             String?            @db.VarChar(1000)
  legislator             legislator         @relation(fields: [legislator_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  parliamentarygroup     parliamentarygroup @relation(fields: [parliamentary_group_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([change_reason], map: "ix_parliamentarymembership_change_reason")
  @@index([legislator_id], map: "ix_parliamentarymembership_legislator_id")
  @@index([parliamentary_group_id], map: "ix_parliamentarymembership_parliamentary_group_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model partyfinancing {
  id                  String            @id @db.VarChar
  financing_report_id String            @db.VarChar
  category            financingcategory @default(INGRESO)
  flow_type           flowtype          @default(I_FPD)
  amount              Decimal           @db.Decimal
  currency            String            @default("PEN") @db.VarChar
  notes               String?
  created_at          DateTime          @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at          DateTime          @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  financingreports    financingreports  @relation(fields: [financing_report_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model person {
  id                       String              @id @db.VarChar
  name                     String              @db.VarChar(150)
  lastname                 String              @db.VarChar(150)
  fullname                 String              @unique(map: "ix_person_fullname") @db.VarChar(300)
  image_url                String?             @db.VarChar
  birth_date               DateTime?           @db.Timestamptz(6)
  profession               String?             @db.VarChar(200)
  detailed_biography       Json?
  technical_education      Json?
  university_education     Json                @default("[]")
  no_university_education  Json?
  postgraduate_education   Json                @default("[]")
  work_experience          Json                @default("[]")
  facebook_url             String?             @db.VarChar
  twitter_url              String?             @db.VarChar
  instagram_url            String?             @db.VarChar
  tiktok_url               String?             @db.VarChar
  created_at               DateTime            @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at               DateTime            @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  secondary_school         Boolean?
  political_role           Json?
  popular_election         Json?
  incomes                  Json?
  assets                   Json?
  dni                      String?             @unique
  gender                   String?
  party_number_rop         String?
  place_of_birth           String?
  image_candidate_url      String?
  education_level          Int                 @default(1) @db.SmallInt
  is_incumbent             Boolean?
  has_criminal_record      Boolean             @default(false)
  has_penal_sentence       Boolean             @default(false)
  has_sanction             Boolean             @default(false)
  is_under_investigation   Boolean             @default(false)
  reinfo_status            String?
  rnas_sanctions           Json[]              @default([])
  has_income               Boolean             @default(false)
  has_assets               Boolean             @default(false)
  has_electoral_experience Boolean             @default(false)
  work_experience_count    Int                 @default(0)
  background               background[]
  candidate                candidate[]
  executive                executive[]
  legislator               legislator[]
  person_embeddings        person_embeddings[]
  triviagame               triviagame[]

  @@index([dni(ops: raw("gin_trgm_ops"))], map: "idx_person_dni_trgm", type: Gin)
  @@index([fullname(ops: raw("gin_trgm_ops"))], map: "idx_person_fullname_trgm", type: Gin)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model person_embeddings {
  id         BigInt                 @id @default(autoincrement())
  person_id  String
  content    String
  chunk_type String
  embedding  Unsupported("vector")?
  metadata   Json?                  @default("{}")
  created_at DateTime?              @default(now()) @db.Timestamptz(6)
  person     person                 @relation(fields: [person_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([chunk_type])
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx1")
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx2")
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx3")
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx4")
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx5")
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx6")
  @@index([chunk_type], map: "person_embeddings_chunk_type_idx7")
  @@index([embedding])
  @@index([embedding], map: "person_embeddings_embedding_idx1")
  @@index([embedding], map: "person_embeddings_embedding_idx2")
  @@index([embedding], map: "person_embeddings_embedding_idx3")
  @@index([embedding], map: "person_embeddings_embedding_idx4")
  @@index([embedding], map: "person_embeddings_embedding_idx5")
  @@index([embedding], map: "person_embeddings_embedding_idx6")
  @@index([embedding], map: "person_embeddings_embedding_idx7")
  @@index([person_id])
  @@index([person_id], map: "person_embeddings_person_id_idx1")
  @@index([person_id], map: "person_embeddings_person_id_idx2")
  @@index([person_id], map: "person_embeddings_person_id_idx3")
  @@index([person_id], map: "person_embeddings_person_id_idx4")
  @@index([person_id], map: "person_embeddings_person_id_idx5")
  @@index([person_id], map: "person_embeddings_person_id_idx6")
  @@index([person_id], map: "person_embeddings_person_id_idx7")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model politicalparty {
  id                                                                    String                @id @db.VarChar
  name                                                                  String                @unique(map: "ix_politicalparty_name") @db.VarChar(200)
  acronym                                                               String?               @db.VarChar(20)
  logo_url                                                              String?               @db.VarChar
  color_hex                                                             String?               @db.VarChar(7)
  active                                                                Boolean
  founder                                                               String?               @db.VarChar(200)
  foundation_date                                                       DateTime?             @db.Date
  ideology                                                              String?               @db.VarChar(200)
  main_office                                                           String?               @db.VarChar(300)
  phone                                                                 String?               @db.VarChar(50)
  email                                                                 String?               @db.VarChar(100)
  website                                                               String?               @db.VarChar(200)
  party_timeline                                                        Json?                 @db.Json
  facebook_url                                                          String?               @db.VarChar
  twitter_url                                                           String?               @db.VarChar
  youtube_url                                                           String?               @db.VarChar
  tiktok_url                                                            String?               @db.VarChar
  total_afiliates                                                       Int?
  created_at                                                            DateTime              @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  updated_at                                                            DateTime              @default(dbgenerated("(now() AT TIME ZONE 'utc'::text)")) @db.Timestamptz(6)
  party_president                                                       String?               @db.VarChar(200)
  purpose                                                               String?               @db.VarChar
  core_values                                                           String?               @db.VarChar
  slogan                                                                String?               @db.VarChar(200)
  legal_cases                                                           Json?                 @db.Json
  government_plan_url                                                   String?
  government_plan_summary                                               Json?                 @db.Json
  government_audio_url                                                  String?
  type                                                                  organizationtype?
  rop                                                                   String?
  alliancecomposition_alliancecomposition_child_org_idTopoliticalparty  alliancecomposition[] @relation("alliancecomposition_child_org_idTopoliticalparty")
  alliancecomposition_alliancecomposition_parent_org_idTopoliticalparty alliancecomposition[] @relation("alliancecomposition_parent_org_idTopoliticalparty")
  candidate                                                             candidate[]
  financingreports                                                      financingreports[]
  legislator                                                            legislator[]
  triviagame                                                            triviagame[]

  @@index([acronym], map: "ix_politicalparty_acronym")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model profiles {
  id              String    @id @db.Uuid
  full_name       String?
  avatar_url      String?
  bio             String?
  role            String?   @default("user")
  created_at      DateTime? @default(now()) @db.Timestamptz(6)
  updated_at      DateTime? @default(now()) @db.Timestamptz(6)
  last_sign_in_at DateTime? @db.Timestamptz(6)
  phone           String?
  company         String?
  website         String?
  is_active       Boolean?  @default(true)

  @@index([created_at])
  @@index([role])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model seatparliamentary {
  id            String      @id @db.VarChar
  chamber       chambertype
  number_seat   Int
  row           Int
  legislator_id String?     @db.VarChar
  created_at    DateTime    @db.Timestamptz(6)
  updated_at    DateTime    @db.Timestamptz(6)
  legislator    legislator? @relation(fields: [legislator_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([legislator_id], map: "ix_seatparliamentary_legislator_id")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model team {
  id            String   @id @db.VarChar
  first_name    String?
  last_name     String?
  linkedin_url  String?
  portfolio_url String?
  is_principal  Boolean? @default(false)
  email         String?
  role          String?
  image_url     String?
  phrase        String?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model triviagame {
  id                 BigInt          @id @default(autoincrement())
  quote              String?
  person_id          String?         @db.VarChar
  options            Json?           @db.Json
  political_party_id String?         @db.VarChar
  category           String?
  difficulty         String?
  created_at         DateTime        @default(now()) @db.Timestamptz(6)
  global_index       BigInt
  explanation        String?
  source_url         String?
  person             person?         @relation(fields: [person_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  politicalparty     politicalparty? @relation(fields: [political_party_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.

model userfeedback {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  created_at       DateTime @default(now()) @db.Timestamptz(6)
  type             String
  status           String   @default("pendiente")
  email            String?
  message          String?
  image_url        String?
  reference_url    String?
  candidate_name   String?
  candidate_url    String?
  correction_field String?
  current_value    String?
  correct_value    String?
  source_url       String?
}

enum attendancestatus {
  ASISTENCIA
  FALTA
  FALTA_JUSTIFICADA
  TARDANZA
  LICENCIA
  COMISION_OFICIAL
}

enum backgroundstatus {
  EN_INVESTIGACION
  SENTENCIADO
  SANCIONADO
  ARCHIVADO
  ABSUELTO
  PRESCRITO
}

enum backgroundtype {
  PENAL
  ETICA
  CIVIL
  ADMINISTRATIVO
}

enum billapprovalstatus {
  PRESENTADO
  EN_COMISION
  DICTAMEN
  EN_AGENDA_PLENO
  ORDEN_DEL_DIA
  EN_CUARTO_INTERMEDIO
  APROBADO_PRIMERA_VOTACION
  PENDIENTE_SEGUNDA_VOTACION
  APROBADO
  AUTOGRAFA
  PUBLICADO
  EN_RECONSIDERACION
  RETORNA_A_COMISION
  AL_ARCHIVO
  DECRETO_ARCHIVO
  RETIRADO_POR_AUTOR
}

enum candidacystatus {
  SOLICITUD_INSCRIPCION
  INSCRITO
  TACHADO
  EXCLUIDO
  IMPROCEDENTE
  RENUNCIA
  APELACION
}

enum candidacytype {
  PRESIDENTE
  VICEPRESIDENTE_1
  SENADOR
  DIPUTADO
  VICEPRESIDENTE_2
  PARLAMENTO_ANDINO
}

enum chambertype {
  CONGRESO
  SENADO
  DIPUTADOS
}

enum endoftermreason {
  RENUNCIA
  REMOCION
  FALLECIMIENTO
  VACANCIA
  PERIODO_FINALIZADO
  DESCONOCIDO
}

enum executiverole {
  PRESIDENTE
  VICEPRESIDENTE
  PRIMER_MINISTRO
  MINISTRO
}

enum financingcategory {
  INGRESO
  GASTO
  DEUDA
}

enum financingstatus {
  DENTRO_DEL_PLAZO
  FUERA_DEL_PLAZO
  NO_PRESENTARON
}

enum flowtype {
  I_FPD
  I_F_PRIVADO
  I_OPERACIONALES
  G_FONDO_FPD
  G_FONDO_F_PRIVADO
  G_OPERACIONALES
  D_TOTAL
}

enum groupchangereason {
  INICIAL
  CAMBIO_VOLUNTARIO
  EXPULSION
  RENUNCIA
  DISOLUCION_BANCADA
  CAMBIO_ESTRATEGICO
  SANCION_DISCIPLINARIA
  OTRO
}

enum legislatorcondition {
  EN_EJERCICIO
  FALLECIDO
  SUSPENDIDO
  LICENCIA
  DESTITUIDO
}

enum organizationtype {
  PARTIDO
  ALIANZA
}

enum sessiontype {
  PLENO
  COMISION_PERMANENTE
  COMISION_ORDINARIA
  EXTRAORDINARIA
}

model User {
  id            String    @id
  name          String
  email         String
  emailVerified Boolean   @default(false)
  image         String?
  role          String    @default("user")
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  sessions      Session[]
  accounts      Account[]

  @@unique([email])
  @@map("user")
}

model Session {
  id        String   @id
  expiresAt DateTime
  token     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  ipAddress String?
  userAgent String?
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([token])
  @@index([userId])
  @@map("session")
}

model Account {
  id                    String    @id
  accountId             String
  providerId            String
  userId                String
  user                  User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  accessToken           String?
  refreshToken          String?
  idToken               String?
  accessTokenExpiresAt  DateTime?
  refreshTokenExpiresAt DateTime?
  scope                 String?
  password              String?
  createdAt             DateTime  @default(now())
  updatedAt             DateTime  @updatedAt

  @@index([userId])
  @@map("account")
}

model Verification {
  id         String   @id
  identifier String
  value      String
  expiresAt  DateTime
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@index([identifier])
  @@map("verification")
}
